import static org.junit.Assert.assertArrayEquals;

import java.net.URI;
import java.net.URL;

import org.junit.Test;

public class DocSearchTest{
    @Test
    public void testHandleRequest(){
        //assertEquals("Don't know how to handle that path!", handleRequest(new URI("")));
    }
}